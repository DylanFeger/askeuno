version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm ci --include=dev
    build:
      - npm run build
run:
  command: NODE_ENV=production node dist/index.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
