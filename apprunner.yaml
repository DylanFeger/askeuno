version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm ci --include=dev
    build:
      - npm run build
run:
  command: node dist/index.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
