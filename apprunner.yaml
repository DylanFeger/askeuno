version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci --include=dev
      - npm run build
run:
  runtime-version: 22
  command: NODE_ENV=production node dist/index.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
